function confirmDeletion() {
    // confirm shows a dialog box with two buttons: OK and Cancel
    // if user clicks OK, confirm returns true which will submit the form
    // if user clicks Cancel, confirm returns false which keeps you on the same page
    return confirm("Are you sure you want to delete this item?");   
}