// Component Class: similar to Router objects in Express
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  // Data (variables that the UI can access)
  title = 'my-app';
  // Behaviour (functions that the UI can trigger)
}
