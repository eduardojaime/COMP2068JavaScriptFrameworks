function openFunction() {
    // everything I need to run this code is inside of this block
    let counter = 1; // variable is created
    counter++;
    console.log(counter);
} // variable is destroyed

openFunction();
openFunction();
// output? 